CREATE TABLE FACTURA (
    N_TICKET INT,
    N_FACTURA INT NOT NULL,
    CEDULA VARCHAR(30)
);

ALTER TABLE PUBLIC.FACTURA ADD CONSTRAINT FACTURA_PK PRIMARY KEY (N_FACTURA);
ALTER TABLE PUBLIC.FACTURA ADD CONSTRAINT FACTURAS_FK FOREIGN KEY (N_TICKET) REFERENCES PUBLIC.TICKET (NUMERO_TICKET);



CREATE TABLE CLIENTE (
    CEDULA VARCHAR(10) NOT NULL,
    NOMBRE VARCHAR(30),
    DIRECCION VARCHAR(30),
    TELEFONO VARCHAR(10),
    E_MAIL VARCHAR(50)
);

ALTER TABLE PUBLIC.CLIENTE ADD CONSTRAINT CLIENTE_PK PRIMARY KEY (CEDULA);

CREATE TABLE TICKET (
    NUMERO_TICKET INT NOT NULL,
    PLACA CHARACTER(30),
    FECHA date,
    HORA_ENTRADA time NOT NULL,
    HORA_SALIDA time
);

ALTER TABLE PUBLIC.TICKET ADD CONSTRAINT SYS_PK_10091 PRIMARY KEY (NUMERO_TICKET);


CODIGO EN JAVA CONEXION CON SQL
INCERTAR TICKET

String sql =  "INSERT INTO TICKET (NUMERO_TICKET, "
				+ "      PLACA, FECHA, HORA_ENTRADA) "
			
				+ "VALUES (?,?,?,?);";
GENERAR UN TICKET

String sql= "SELECT MAX (NUMERO_TICKET) AS NUMERO_TICKET"
				+"		FROM TICKET";

	String sql="UPDATE TICKET " 
			+"SET HORA_SALIDA=?, "
			+"TIEMPO=? "
			+"WHERE NUMERO_TICKET=? " ;



